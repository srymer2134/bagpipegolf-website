---
/**
 * Social media link row for Bagpipe Golf.
 *
 * Used in the hero (index.astro) and the site-wide Footer. Two size
 * variants — `hero` (40px, prominent) and `footer` (32px, muted).
 *
 * Handles claimed 2026-09-10; plain `@bagpipegolf` was unavailable on
 * Instagram + X, hence the `-app` / `-1` variants. See
 * `.claude/projects/-Users-bacon-fairwayiq-flutter/memory/reference_social_handles.md`.
 *
 * Click tracking: every anchor carries `data-social-platform` and
 * `data-social-source` attributes. A tiny delegated listener at the
 * bottom of this component fires a `bpg:social_click` CustomEvent on
 * `window`; any analytics library (PostHog, Plausible, GA4, etc.) can
 * attach to the same listener without touching this component again.
 * No analytics tool is currently wired up — the event fires but nobody
 * listens yet. That's deliberate: the plumbing lands now, the tool
 * choice is Sam's later.
 */

interface Props {
  variant?: 'hero' | 'footer';
  source?: string;
}

const { variant = 'hero', source = variant } = Astro.props;
const size = variant === 'hero' ? 22 : 18;
const rootClass = `social social--${variant}`;

const links = [
  {
    platform: 'tiktok',
    href: 'https://www.tiktok.com/@bagpipegolf',
    label: 'Bagpipe Golf on TikTok (@bagpipegolf)',
    path: 'M19.6 7.3a5.4 5.4 0 0 1-3.2-1.1 5.5 5.5 0 0 1-2.2-3.7h-3.4v13.2a2.4 2.4 0 1 1-2.4-2.4c.3 0 .5 0 .8.1V9.9a5.9 5.9 0 1 0 5 5.8V10a8.7 8.7 0 0 0 5.4 1.8V8.4c-.3 0-.5-.1-.8-.1a5.6 5.6 0 0 1 .8-1z',
  },
  {
    platform: 'instagram',
    href: 'https://www.instagram.com/bagpipegolfapp/',
    label: 'Bagpipe Golf on Instagram (@bagpipegolfapp)',
    path: 'M12 2.2c3.2 0 3.6 0 4.8.1 1.2.1 1.8.3 2.2.5.6.2 1 .5 1.4.9.4.4.7.8.9 1.4.2.4.4 1 .5 2.2.1 1.2.1 1.6.1 4.8s0 3.6-.1 4.8c-.1 1.2-.3 1.8-.5 2.2-.2.6-.5 1-.9 1.4-.4.4-.8.7-1.4.9-.4.2-1 .4-2.2.5-1.2.1-1.6.1-4.8.1s-3.6 0-4.8-.1c-1.2-.1-1.8-.3-2.2-.5a3.8 3.8 0 0 1-1.4-.9 3.8 3.8 0 0 1-.9-1.4c-.2-.4-.4-1-.5-2.2-.1-1.2-.1-1.6-.1-4.8s0-3.6.1-4.8c.1-1.2.3-1.8.5-2.2.2-.6.5-1 .9-1.4.4-.4.8-.7 1.4-.9.4-.2 1-.4 2.2-.5 1.2-.1 1.6-.1 4.8-.1zm0 3.4a6.4 6.4 0 1 0 0 12.8 6.4 6.4 0 0 0 0-12.8zm0 10.6a4.2 4.2 0 1 1 0-8.4 4.2 4.2 0 0 1 0 8.4zm6.7-10.9a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z',
  },
  {
    platform: 'youtube',
    href: 'https://www.youtube.com/@bagpipegolf',
    label: 'Bagpipe Golf on YouTube (@bagpipegolf)',
    path: 'M23.5 6.2a3 3 0 0 0-2.1-2.1C19.5 3.5 12 3.5 12 3.5s-7.5 0-9.4.5A3 3 0 0 0 .5 6.1C0 8 0 12 0 12s0 4 .5 5.9c.3 1 1.1 1.8 2.1 2 1.9.6 9.4.6 9.4.6s7.5 0 9.4-.5a3 3 0 0 0 2.1-2.1C24 16 24 12 24 12s0-4-.5-5.8zM9.6 15.6V8.4l6.2 3.6-6.2 3.6z',
  },
  {
    platform: 'x',
    href: 'https://x.com/bagpipegolf1',
    label: 'Bagpipe Golf on X (@bagpipegolf1)',
    path: 'M18.9 2.5h3.3l-7.2 8.2 8.4 11.1h-6.6l-5.1-6.7-5.9 6.7H2.5l7.7-8.8L2.2 2.5h6.8l4.6 6.1 5.3-6.1zm-1.2 17.4h1.8L6.4 4.3H4.5l13.2 15.6z',
  },
];
---

<nav class={rootClass} aria-label="Bagpipe Golf on social media">
  {
    links.map((l) => (
      <a
        href={l.href}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={l.label}
        class="social__link"
        data-social-platform={l.platform}
        data-social-source={source}
      >
        <svg viewBox="0 0 24 24" width={size} height={size} aria-hidden="true">
          <path fill="currentColor" d={l.path} />
        </svg>
      </a>
    ))
  }
</nav>

<script>
  // Delegated click tracker. Fires `bpg:social_click` on window with
  // { platform, source, path, ts }. Any analytics tool (PostHog,
  // Plausible, GA4) can subscribe:
  //
  //   window.addEventListener('bpg:social_click', (e) => {
  //     posthog?.capture('social_click', e.detail);
  //   });
  //
  // No listener is wired up today — the plumbing lands now, the
  // analytics tool choice is Sam's later. Runs once per page load
  // regardless of how many SocialLinks components render.
  if (!(window as any).__bpgSocialClickTracker) {
    (window as any).__bpgSocialClickTracker = true;
    document.addEventListener('click', (e) => {
      const target = e.target as HTMLElement | null;
      const link = target?.closest?.('[data-social-platform]') as
        | HTMLAnchorElement
        | null;
      if (!link) return;
      const detail = {
        platform: link.dataset.socialPlatform,
        source: link.dataset.socialSource,
        path: window.location.pathname,
        ts: Date.now(),
      };
      window.dispatchEvent(new CustomEvent('bpg:social_click', { detail }));
    });
  }
</script>

<style>
  .social {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }
  .social--hero {
    margin-top: 18px;
    justify-content: center;
  }
  .social--footer {
    margin-top: 12px;
    gap: 10px;
  }
  .social__link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    text-decoration: none;
    transition:
      background 0.15s ease,
      color 0.15s ease,
      transform 0.08s ease,
      box-shadow 0.15s ease;
  }
  .social--hero .social__link {
    width: 40px;
    height: 40px;
    border: 1.5px solid var(--brand-green);
    color: var(--brand-green);
    background: transparent;
  }
  .social--hero .social__link:hover,
  .social--hero .social__link:focus-visible {
    background: var(--brand-green);
    color: #fff;
    transform: translateY(-1px);
    box-shadow: 0 6px 14px rgba(26, 92, 56, 0.22);
    outline: none;
  }
  .social--hero .social__link:focus-visible {
    box-shadow:
      0 0 0 3px rgba(26, 92, 56, 0.28),
      0 6px 14px rgba(26, 92, 56, 0.22);
  }
  .social--footer .social__link {
    width: 32px;
    height: 32px;
    border: 1px solid var(--line);
    color: var(--ink-muted);
    background: var(--paper);
  }
  .social--footer .social__link:hover,
  .social--footer .social__link:focus-visible {
    color: var(--brand-green-dark);
    border-color: var(--brand-green);
    outline: none;
  }
  .social__link svg {
    display: block;
  }
</style>
